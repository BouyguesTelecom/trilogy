/**
 * Popover direction
 */
export enum PopoverDirection {
  BOTTOM = 'bottom',
  LEFT = 'left',
  RIGHT = 'right',
}

/**
 * Popover direction values
 */
export type PopoverDirectionValues = `${PopoverDirection}`

/**
 * Popover arrow
 */
export enum PopoverArrowPosition {
  START = 'start',
  END = 'end',
}

/**
 * Popover arrow values
 */
export type PopoverArrowPositionValues = `${PopoverArrowPosition}`
