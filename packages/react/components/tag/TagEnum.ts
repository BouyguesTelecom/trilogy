/**
 * List of variants for Tag
 */
export enum TagVariant {
  DEFAULT = 'DEFAULT',
  SUCCESS = 'SUCCESS',
  INFO = 'INFO',
  ERROR = 'ERROR',
  WARNING = 'WARNING',
}

/**
 * List of variants values for Tag
 */
export type TagVariantValues = `${TagVariant}`

/**
 * List of variants background styles for Tag
 */
