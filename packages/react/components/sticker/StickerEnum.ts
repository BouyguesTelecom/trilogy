/**
 * List of variant for Sticker
 */
export enum StickerVariant {
  PRIMARY = 'primary',
  SUCCESS = 'success',
  INFO = 'info',
}

/**
 * List of variant for Sticker values
 */
export type StickerVariantValues = `${StickerVariant}`

/**
 * List of available Markup
 */
export enum StickerMarkup {
  DIV = 'div',
  P = 'p',
  SPAN = 'span',
}

/**
 * List of available Markup values
 */
export type StickerMarkupValues = `${StickerMarkup}`
